# Sobrescreve DATABASE_URL do .env legado para o Postgres do docker-compose (host 5442).
# Uso: npm run test:integration  →  dotenv -e .env -e .env.test
DATABASE_URL="postgresql://agendai:agendai123@localhost:5442/agendai_db"
NODE_ENV=test
